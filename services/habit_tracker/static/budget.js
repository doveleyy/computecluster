"use strict";
const config=window.habitConfig,api=path=>`${config.basePath}/api/budget${path}`;
    const state={kind:'daily_spend',category:'other',summary:null,editingBudget:false,resetTimer:null};
    const categoryLabels=Object.fromEntries(config.categories.map(c=>[c.value,c.label]));
    const categorised=new Set(config.categorisedKinds);
    const money=new Intl.NumberFormat('en-SG',{style:'currency',currency:config.currency,minimumFractionDigits:2});
    const labels={daily_spend:'Daily spending',fund_redemption:'Fund redemption',fund_contribution:'Fund contribution'};
    const help={daily_spend:'Counts against today’s allowance. Any remaining amount settles into the fund at midnight.',fund_redemption:'Pays directly from the sinking fund without changing today’s allowance.',fund_contribution:'Adds money directly to the sinking fund and records where it came from.'};
    document.querySelector('#user').textContent=config.displayName;document.querySelector('#timezone').textContent=config.timezone;
    const format=cents=>money.format(cents/100),signed=cents=>`${cents>=0?'+':'−'}${format(Math.abs(cents))}`;
    async function request(path,options={}){const response=await fetch(api(path),{headers:{'Content-Type':'application/json'},...options});if(!response.ok){let detail=`Request failed (${response.status})`;try{detail=(await response.json()).detail||detail}catch{}throw new Error(detail)}return response.status===204?null:response.json()}
    function busy(value){document.querySelectorAll('button').forEach(button=>button.disabled=value)}function show(text,error=false){const node=document.querySelector('#message');node.textContent=text;node.style.color=error?'var(--danger)':'var(--muted)'}
    function renderMode(){document.querySelectorAll('[data-kind]').forEach(button=>{const active=button.dataset.kind===state.kind;button.classList.toggle('active',active);button.setAttribute('aria-pressed',String(active))});document.querySelector('#mode-help').textContent=help[state.kind];(function(){var on=categorised.has(state.kind),g=document.querySelector('#category-picker');g.classList.toggle('unavailable',!on);g.toggleAttribute('inert',!on);g.setAttribute('aria-disabled',String(!on))})()}
    function renderCategories(){const root=document.querySelector('#categories');root.innerHTML='';for(const item of config.categories){const button=document.createElement('button');button.type='button';button.dataset.category=item.value;const active=item.value===state.category;button.className=active?'active':'';button.setAttribute('aria-pressed',String(active));button.textContent=item.label;root.append(button)}}
    function renderSummary(summary){state.summary=summary;const remaining=document.querySelector('#remaining');remaining.textContent=format(summary.daily_remaining_cents);remaining.classList.toggle('negative',summary.daily_remaining_cents<0);document.querySelector('#budget-value').textContent=format(summary.daily_budget_cents);document.querySelector('#budget-figure').textContent=format(summary.daily_budget_cents);document.querySelector('#spent').textContent=format(summary.daily_spent_cents);const settlement=document.querySelector('#settlement');settlement.textContent=signed(summary.daily_remaining_cents);settlement.className=summary.daily_remaining_cents<0?'negative':'positive';const fund=document.querySelector('#fund');fund.textContent=format(summary.fund_balance_cents);fund.classList.toggle('negative',summary.fund_balance_cents<0);document.querySelector('#pending').textContent=summary.pending_surplus_cents?`+${format(summary.pending_surplus_cents)}`:'—';const ratio=summary.daily_budget_cents?Math.max(0,summary.daily_remaining_cents)/summary.daily_budget_cents:0;const fill=document.querySelector('#fill');fill.style.width=`${Math.min(100,ratio*100)}%`;fill.classList.toggle('over',summary.daily_remaining_cents<0);fill.setAttribute('aria-valuenow',String(Math.max(0,summary.daily_remaining_cents)));fill.setAttribute('aria-valuemax',String(summary.daily_budget_cents));if(!state.editingBudget)document.querySelector('#budget-input').value=(summary.daily_budget_cents/100).toFixed(2);renderActivity(summary.transactions);scheduleReset(summary.next_reset_at)}
    function renderActivity(transactions){document.querySelector('#activity-count').textContent=`${transactions.length} ${transactions.length===1?'entry':'entries'}`;const root=document.querySelector('#activity');root.innerHTML='';if(!transactions.length){root.innerHTML='<div class="empty">Nothing recorded today.</div>';return}for(const item of transactions){const row=document.createElement('div');row.className='entry';const positive=item.kind==='fund_contribution',timestamp=new Date(item.occurred_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});const meta=categorised.has(item.kind)?`${labels[item.kind]} · ${categoryLabels[item.category]||'Other'} · ${timestamp}`:`${labels[item.kind]} · ${timestamp}`;row.innerHTML=`<div class="entry-main"><div class="entry-name"></div><div class="entry-meta">${meta}</div></div><div class="entry-side"><span class="entry-amount ${positive?'positive':''}">${positive?'+':'−'}${format(item.amount_cents)}</span><button class="undo" data-delete="${item.id}">UNDO</button></div>`;row.querySelector('.entry-name').textContent=item.description;root.append(row)}}
    function renderHistory(data){const root=document.querySelector('#history');root.innerHTML='';const days=data.days.slice(-7),ceiling=Math.max(1,...days.map(day=>Math.max(day.budget_cents,day.spent_cents)));for(const day of days){const date=new Date(`${day.date}T00:00:00`),height=Math.max(2,Math.min(100,(day.spent_cents/ceiling)*100)),cell=document.createElement('div');cell.className='day';cell.title=`${day.date}: spent ${format(day.spent_cents)} of ${format(day.budget_cents)}`;cell.innerHTML=`<div class="bar-track"><span class="bar ${day.spent_cents>day.budget_cents?'over':''}" style="height:${height}%"></span></div><span class="day-value">${format(day.spent_cents)}</span><span class="day-name">${date.toLocaleDateString([],{weekday:'narrow'})}</span>`;root.append(cell)}}
    function renderLedger(data){const root=document.querySelector('#ledger');root.innerHTML='';if(!data.entries.length){root.innerHTML='<div class="empty">No budget activity yet.</div>';return}for(const item of data.entries){const row=document.createElement('div');row.className='ledger-row';const positive=item.amount_cents>=0,isAdjustment=item.kind==='daily_budget_adjustment',note=isAdjustment?`Daily budget ${format(item.previous_amount_cents)} → ${format(item.new_amount_cents)}`:item.description,when=item.occurred_at?new Date(item.occurred_at).toLocaleString([],{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}):item.date;row.innerHTML=`<span class="ledger-note"></span><span class="ledger-amount ${positive?'positive':'negative'}">${signed(item.amount_cents)}</span>`;row.querySelector('.ledger-note').textContent=`${when} · ${note}`;root.append(row)}}
    function scheduleReset(nextReset){clearTimeout(state.resetTimer);const wait=Math.max(1000,new Date(nextReset).getTime()-Date.now()+1000);state.resetTimer=setTimeout(refresh,Math.min(wait,2147483647))}async function refresh(){const[summary,history,ledger]=await Promise.all([request('/summary'),request('/history?days=7'),request('/ledger?limit=50')]);renderSummary(summary);renderHistory(history);renderLedger(ledger)}
    function editBudget(editing){state.editingBudget=editing;document.querySelector('#budget-trigger').hidden=editing;document.querySelector('#budget-form').hidden=!editing;if(editing){const input=document.querySelector('#budget-input');input.value=(state.summary.daily_budget_cents/100).toFixed(2);input.focus();input.select()}}
    document.querySelector('#modes').addEventListener('click',event=>{const kind=event.target.dataset.kind;if(!kind)return;state.kind=kind;renderMode()});document.querySelector('#transaction-form').addEventListener('submit',async event=>{event.preventDefault();const amount=Math.round(Number(document.querySelector('#amount').value)*100),description=document.querySelector('#description').value;busy(true);show('Saving…');try{await request('/transactions',{method:'POST',body:JSON.stringify({kind:state.kind,amount_cents:amount,description,category:categorised.has(state.kind)?state.category:'other'})});document.querySelector('#amount').value='';document.querySelector('#description').value='';await refresh();show(`${labels[state.kind]} saved.`)}catch(error){show(error.message,true)}finally{busy(false)}});document.querySelector('#activity').addEventListener('click',async event=>{const id=event.target.dataset.delete;if(!id||!confirm('Permanently remove this entry?'))return;busy(true);try{await request(`/transactions/${id}`,{method:'DELETE'});await refresh();show('Entry removed.')}catch(error){show(error.message,true)}finally{busy(false)}});document.querySelector('#budget-trigger').addEventListener('click',()=>editBudget(true));document.querySelector('#budget-input').addEventListener('keydown',event=>{if(event.key==='Escape')editBudget(false)});document.querySelector('#budget-form').addEventListener('submit',async event=>{event.preventDefault();const amount=Math.round(Number(document.querySelector('#budget-input').value)*100);busy(true);try{await request('/daily-budget',{method:'PUT',body:JSON.stringify({amount_cents:amount})});editBudget(false);await refresh();show('Daily budget updated from today.')}catch(error){show(error.message,true)}finally{busy(false)}});document.querySelector('#categories').addEventListener('click',event=>{const category=event.target.dataset.category;if(!category)return;state.category=category;renderCategories()});
    renderMode();renderCategories();refresh().catch(error=>show(error.message,true));
